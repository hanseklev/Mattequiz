<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.s341872">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher_abacus_colour"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_abacus_colour_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mattequiz.NoActionBar">
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:parentActivityName=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.game.GameActivity"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity
            android:name=".ui.stats.StatsActivity"
            android:label="@string/show_stats"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity
            android:name=".ui.preferences.PreferencesActivity"
            android:label="@string/preferences"
            android:parentActivityName=".ui.main.MainActivity" />
    </application>

</manifest>